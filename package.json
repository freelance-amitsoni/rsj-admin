{
  "name": "rsj-backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:customer": "cd frontends/customer && npm install && npm run build",
    "build:admin": "cd frontends/admin && npm install && npm run build",
    "build:all": "npm run build:customer && npm run build:admin"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "http-proxy": "^1.18.1",
    "jsonwebtoken": "^9.0.3",
    "localtunnel": "^2.0.2",
    "mongoose": "^9.1.5",
    "socket.io": "^4.8.3"
  }
}